# Optional — set these for real AI and Razorpay integration.
# Without them, the app uses rule-based fallback and dry-run mode.

# Gemini AI (for diagnosis and decision)
GEMINI_API_KEY=
GEMINI_MODEL_ID=gemini-2.5-flash

# Razorpay (test mode only!)
RAZORPAY_KEY_ID=rzp_test_xxxxx
RAZORPAY_KEY_SECRET=xxxxx
RAZORPAY_WEBHOOK_SECRET=

# Follow-Up Scheduler (Feature 7 re-loop: 1 to enable autonomous background loop)
ENABLE_FOLLOW_UP_SCHEDULER=0
FOLLOW_UP_POLL_SECONDS=3600

# Database (defaults to SQLite if not set)
# DATABASE_URL=sqlite:///./recovery.db
